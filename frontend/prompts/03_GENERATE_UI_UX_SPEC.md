# Generate UI UX Spec

Use this prompt after audit and before major implementation.

## Prompt

Read:

- `AGENTS.md`
- `frontend/RULES.md`
- all files under `frontend/docs/framework/`
- all files under `frontend/docs/generated/`

Then refresh:

- `frontend/docs/generated/UI_UX_SPEC.md`
- `frontend/docs/generated/WIREFRAMES.md`
- `frontend/docs/generated/DECISIONS.md` if new tradeoffs are introduced

Focus on:

- experience goals
- visual direction for the current scope
- screen structure
- user actions and states
- responsive considerations
- consistency with existing reusable components
